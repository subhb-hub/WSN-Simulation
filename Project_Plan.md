总结来说：这次课外作业要求你**理解 LEACH 算法，用 Python 3.13 编写一个 WSN 无线传感器网络节点分簇聚合模拟程序，并给出程序运行结果图**。项目不一定要做到很复杂，但至少要体现：节点随机分布、每轮选择簇头、普通节点加入最近簇头、簇头完成数据聚合并向基站发送数据。

## 一、任务要求总结

根据投影内容，作业题目是：

**WSN——节点分簇聚合模拟**

需要完成的内容主要包括五部分：

### 1. 实验目的

你需要说明：

了解 LEACH 算法的基本思想；

掌握如何用 Python 实现一个 WSN 节点分簇聚合过程；

运行程序并给出仿真结果图。

LEACH 的核心思想是通过随机轮换簇头节点，让不同节点分担高能耗通信任务，从而延长无线传感器网络生命周期。原始 LEACH 论文将其描述为一种自组织、自适应的聚类协议，通过随机化方式分配簇头能耗。([MIT CSAIL PDOS][1])

### 2. 实验环境要求

需要熟悉 Python 基本编程环境，投影中提到的实验环境是：

Python 3.13；

IDE 可使用 PyCharm 或 Anaconda；

需要能够运行 Python 程序并生成实验结果图。

Python 官网已经提供 Python 3.13 系列版本下载，投影中的 Python 3.13 要求与官方发布版本一致。([Python.org][2])

### 3. 实验原理要求

报告中需要介绍：

WSN，即 Wireless Sensor Network，无线传感器网络；

LEACH 算法的基本流程；

给定 WSN 节点数量后，让节点随机分布；

根据 LEACH 算法，在每一轮对节点进行分簇；

普通节点将数据发送给簇头，簇头进行数据聚合后发送给基站。

### 4. 程序要求

需要提交一份完整 Python 程序，程序至少应包含：

节点随机生成；

簇头选择；

节点分簇；

数据聚合过程模拟；

结果可视化。

### 5. 实验结果要求

需要给出程序运行结果图，例如：

节点分布图；

簇头节点标记图；

普通节点到簇头的连线图；

簇头到基站的通信图；

每一轮簇头数量变化图；

网络剩余能量变化图；

存活节点数量变化图。

---

## 二、项目方案设计

项目名称可以写成：

**基于 Python 的 WSN 节点分簇聚合模拟系统设计与实现**

这个项目建议做成一个“小型仿真系统”，而不是只画一张散点图。这样报告内容会更完整，也更符合“节点分簇聚合模拟”的要求。

---

## 三、系统总体思路

整个程序可以按照以下流程运行：

```text
初始化参数
↓
随机生成 N 个传感器节点
↓
设置基站位置
↓
进入多轮仿真
↓
每一轮根据 LEACH 阈值选择簇头
↓
普通节点选择距离最近的簇头加入
↓
普通节点向簇头发送数据
↓
簇头进行数据聚合
↓
簇头向基站发送聚合数据
↓
更新节点能量
↓
统计并绘制结果
```

---

## 四、LEACH 算法设计

### 1. 节点模型

每个传感器节点可以包含以下属性：

```text
节点编号 id
节点坐标 x, y
剩余能量 energy
是否存活 alive
是否为簇头 is_cluster_head
所属簇头 cluster_id
上一轮成为簇头的时间 last_ch_round
```

### 2. 网络模型

可以设置一个二维区域，例如：

```text
区域大小：100 × 100
节点数量：100
基站位置：(50, 120)
初始能量：0.5 J
簇头概率 p = 0.05
仿真轮数 rounds = 100
```

基站可以放在网络区域外部，这样更能体现簇头远距离通信的高能耗问题。

### 3. 簇头选择

LEACH 中每个节点不是固定簇头，而是在每一轮根据概率竞争成为簇头。常用阈值为：

[
T(n)=
\begin{cases}
\frac{p}{1-p(r \bmod \frac{1}{p})}, & n \in G \
0, & n \notin G
\end{cases}
]

其中：

```text
p：期望簇头比例
r：当前轮数
G：最近 1/p 轮内没有当过簇头的节点集合
```

如果某个节点产生的随机数小于阈值 T(n)，则该节点成为本轮簇头。

### 4. 分簇过程

簇头选出后，普通节点计算自己到所有簇头的距离，并加入距离最近的簇头。

分簇规则：

```text
对于每个普通节点：
    计算它到每个簇头的欧氏距离
    选择距离最近的簇头
    将该节点加入该簇
```

### 5. 数据聚合过程

可以采用简化模拟：

```text
普通节点发送 1 个数据包给簇头；
簇头接收多个数据包；
簇头将多个数据包聚合成 1 个数据包；
簇头将聚合后的数据发送给基站。
```

这样可以体现“聚合”的意义：减少直接发送到基站的数据量。

---

## 五、能量模型设计

如果想让项目更完整，建议加入简单能量模型。

无线通信中常用的一阶无线电能耗模型可以设为：

```text
发送 k bit 数据到距离 d：
E_tx = k * E_elec + k * epsilon_fs * d^2

接收 k bit 数据：
E_rx = k * E_elec

数据聚合能耗：
E_da = k * E_DA
```

可以设置参数：

```text
E_elec = 50 nJ/bit
epsilon_fs = 10 pJ/bit/m²
E_DA = 5 nJ/bit
packet_size = 4000 bit
```

普通节点发送数据会消耗能量，簇头接收数据、聚合数据、向基站发送数据也会消耗能量。当节点能量小于等于 0 时，节点死亡，不再参与后续轮次。

---

## 六、程序模块划分

建议把程序设计成以下几个模块：

### 1. 参数配置模块

负责设置实验参数：

```python
NUM_NODES = 100
AREA_WIDTH = 100
AREA_HEIGHT = 100
BASE_STATION = (50, 120)
P = 0.05
ROUNDS = 100
INIT_ENERGY = 0.5
```

### 2. 节点类 Node

负责保存每个节点的信息：

```python
class Node:
    def __init__(self, node_id, x, y, energy):
        self.id = node_id
        self.x = x
        self.y = y
        self.energy = energy
        self.alive = True
        self.is_cluster_head = False
        self.cluster_id = None
        self.last_ch_round = -999
```

### 3. 网络初始化模块

功能：

```text
随机生成节点坐标；
初始化节点能量；
设置基站位置。
```

### 4. 簇头选择模块

功能：

```text
根据 LEACH 阈值选择簇头；
避免同一节点短时间内频繁成为簇头；
如果某一轮没有簇头，则随机选择一个存活节点作为簇头。
```

### 5. 分簇模块

功能：

```text
普通节点寻找最近簇头；
建立“簇头—成员节点”映射关系；
记录每个节点所属簇。
```

### 6. 能量更新模块

功能：

```text
普通节点发送数据消耗能量；
簇头接收数据消耗能量；
簇头聚合数据消耗能量；
簇头向基站发送数据消耗能量；
判断节点是否死亡。
```

### 7. 结果统计模块

需要统计：

```text
每一轮簇头数量；
每一轮存活节点数量；
每一轮总剩余能量；
每一轮死亡节点数量；
每个簇的节点数量。
```

### 8. 可视化模块

建议至少画两类图。

第一类是某一轮的分簇结果图：

```text
普通节点：蓝色点
簇头节点：红色星形
基站：黑色三角形
普通节点到簇头：灰色线
簇头到基站：红色线
```

第二类是多轮统计图：

```text
轮数—存活节点数量曲线
轮数—网络总剩余能量曲线
轮数—簇头数量变化曲线
```

---

## 七、推荐实验参数

可以在报告中使用如下参数：

| 参数           |      含义 |       推荐值 |
| ------------ | ------: | --------: |
| NUM_NODES    | 传感器节点数量 |       100 |
| AREA_WIDTH   |    区域宽度 |       100 |
| AREA_HEIGHT  |    区域高度 |       100 |
| BASE_STATION |    基站位置 | (50, 120) |
| P            |    簇头概率 |      0.05 |
| ROUNDS       |    仿真轮数 |       100 |
| INIT_ENERGY  |    初始能量 |     0.5 J |
| PACKET_SIZE  |   数据包大小 |  4000 bit |

---

## 八、实验结果应该怎么展示

你的实验结果部分可以这样安排：

### 1. 节点初始分布图

说明节点是在二维区域内随机生成的，模拟 WSN 节点随机部署场景。

### 2. 某一轮 LEACH 分簇图

展示普通节点、簇头、基站以及通信路径。

可以选第 1 轮、第 10 轮、第 50 轮分别展示，说明簇头会随轮次变化。

### 3. 存活节点数量变化图

横轴为轮数，纵轴为存活节点数量。

分析重点：

```text
随着通信轮数增加，节点能量逐渐消耗；
部分节点死亡；
存活节点数量逐渐下降。
```

### 4. 网络总剩余能量变化图

横轴为轮数，纵轴为总剩余能量。

分析重点：

```text
网络能量随轮数增加逐渐下降；
簇头节点由于承担数据接收、聚合和远距离发送任务，能量消耗更快；
LEACH 通过轮换簇头减轻单个节点长期高能耗的问题。
```

### 5. 簇头数量变化图

横轴为轮数，纵轴为簇头数量。

分析重点：

```text
簇头数量不是固定值；
平均簇头数量接近 NUM_NODES × P；
随机性会导致不同轮次簇头数量存在波动。
```

---

## 九、实验报告结构建议

你的报告可以按下面结构写：

```text
一、实验目的
1. 了解 WSN 的基本概念
2. 理解 LEACH 分簇聚合算法
3. 使用 Python 实现 WSN 节点分簇聚合模拟
4. 通过可视化结果分析算法效果

二、实验环境
1. Python 3.13
2. PyCharm / Anaconda
3. numpy
4. matplotlib

三、实验原理
1. WSN 简介
2. LEACH 算法简介
3. 簇头选择机制
4. 节点分簇机制
5. 数据聚合与能量消耗模型

四、程序设计
1. 参数设置
2. 节点类设计
3. 初始化网络
4. 簇头选择函数
5. 节点分簇函数
6. 能量更新函数
7. 可视化函数

五、实验结果
1. 节点初始分布图
2. LEACH 分簇结果图
3. 存活节点变化图
4. 剩余能量变化图
5. 簇头数量变化图

六、结果分析
1. LEACH 如何完成分簇
2. 簇头轮换的作用
3. 数据聚合如何降低通信开销
4. 网络能量变化规律

七、实验总结
说明本实验实现了 WSN 节点分簇聚合模拟，理解了 LEACH 算法的基本流程，并通过 Python 可视化展示了分簇和能量变化过程。
```

---

## 十、项目完成标准

最低完成标准：

```text
能随机生成节点；
能选出簇头；
能完成普通节点加入最近簇头；
能画出分簇结果图；
能提交完整 Python 程序和实验报告。
```

较好完成标准：

```text
加入多轮仿真；
加入能量消耗模型；
统计存活节点数量；
统计网络剩余能量；
绘制多轮变化曲线。
```

优秀完成标准：

```text
实现完整 LEACH 阈值公式；
展示不同轮次的簇头变化；
比较 LEACH 与普通直接传输的能量差异；
代码结构清晰，函数划分明确；
报告中有算法解释、代码说明和结果分析。
```

---

## 十一、最终建议

这个作业最好不要只做“静态聚类图”，而是做成“多轮 LEACH 仿真”。核心亮点可以写成：

**本项目使用 Python 模拟 WSN 节点随机部署场景，通过 LEACH 算法动态选择簇头，实现普通节点到簇头、簇头到基站的数据聚合传输过程，并进一步引入能量消耗模型，观察网络生命周期、剩余能量和簇头数量随通信轮数的变化。**

这样写，既满足课堂要求，也显得项目完整。

[1]: https://pdos.csail.mit.edu/archive/decouto/papers/heinzelman00.pdf?utm_source=chatgpt.com "Energy-Efficient Communication Protocol for Wireless ..."
[2]: https://www.python.org/downloads/release/python-3130/?utm_source=chatgpt.com "Python Release Python 3.13.0"
