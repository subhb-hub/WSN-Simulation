下面是一份可以直接复制到 Word / WPS 的 **项目方案文档**。我按“任务/作业项目”理解来写，主题为 **WSN 节点分簇聚合模拟：基于 LEACH 协议的 Python 仿真实现**。

---

# WSN 节点分簇聚合模拟项目方案文档

## 一、项目名称

**基于 Python 的 WSN 无线传感器网络节点分簇聚合模拟——LEACH 协议仿真实现**

---

## 二、项目背景

无线传感器网络，即 Wireless Sensor Network，简称 WSN，是由大量低功耗传感器节点组成的分布式网络系统。传感器节点通常部署在监测区域内，用于采集环境数据，例如温度、湿度、光照、压力、位置信息等，并将数据传输到基站。

由于传感器节点通常依靠电池供电，能量有限，因此如何降低通信能耗、延长网络生命周期，是 WSN 研究中的核心问题之一。

LEACH 协议，即 **Low-Energy Adaptive Clustering Hierarchy**，是一种经典的低功耗自适应分簇层次路由协议。它通过周期性地选择部分节点作为簇头节点，使普通节点先将数据发送给簇头，再由簇头进行数据聚合并发送给基站，从而减少网络中的远距离通信次数，降低整体能耗。

本项目拟使用 Python 对 LEACH 协议进行模拟，实现无线传感器节点的随机部署、簇头选举、节点入簇、数据聚合与能量消耗统计，并通过图像展示网络分簇结果和节点能量变化情况。

---

## 三、项目目标

本项目的主要目标如下：

1. 使用 Python 构建无线传感器网络仿真环境，在二维平面中随机生成传感器节点和基站。

2. 实现 LEACH 协议的核心流程，包括簇头选举、普通节点选择最近簇头、簇内数据传输、簇头数据聚合和簇头向基站发送数据。

3. 建立简单的能量消耗模型，模拟节点在发送、接收和聚合数据过程中的能量变化。

4. 通过 `matplotlib` 绘制节点分布图、簇头节点图、簇结构图以及网络剩余能量变化曲线。

5. 输出完整可运行的 Python 程序，并形成实验结果截图或图表，用于作业提交。

---

## 四、项目内容

### 4.1 网络场景初始化

在二维区域内随机部署若干个传感器节点。每个节点具有以下属性：

* 节点编号；
* 横坐标与纵坐标；
* 初始能量；
* 当前是否存活；
* 当前是否为簇头；
* 所属簇头编号；
* 最近一次成为簇头的轮次。

仿真区域可以设定为：

```text
100 m × 100 m
```

基站可以设置在区域中心或区域外部，例如：

```text
基站坐标：(50, 150)
```

节点数量可以设置为：

```text
100 个传感器节点
```

---

### 4.2 LEACH 簇头选举机制

LEACH 协议中，每一轮都会有一定概率的节点成为簇头。为了避免同一个节点频繁成为簇头，LEACH 使用阈值函数控制簇头选举。

簇头选举阈值公式为：

[
T(n)=
\begin{cases}
\frac{p}{1-p \times (r \bmod \frac{1}{p})}, & n \in G \
0, & n \notin G
\end{cases}
]

其中：

* (p)：期望簇头比例；
* (r)：当前轮数；
* (G)：最近 (1/p) 轮内没有当过簇头的节点集合；
* (T(n))：节点 (n) 在当前轮成为簇头的阈值。

每个符合条件的节点生成一个 0 到 1 之间的随机数。如果随机数小于 (T(n))，则该节点成为本轮簇头。

---

### 4.3 节点入簇机制

簇头选举完成后，所有非簇头节点根据距离选择加入最近的簇头。

对于普通节点 (i) 和簇头节点 (j)，两者之间的欧氏距离为：

[
d(i,j)=\sqrt{(x_i-x_j)^2+(y_i-y_j)^2}
]

普通节点会选择距离自己最近的簇头作为所属簇头，并在后续通信阶段将数据发送给该簇头。

如果某一轮没有产生簇头，可以采用以下处理方法：

1. 随机选择一个存活节点作为簇头；
2. 或者跳过当前轮；
3. 或者提高当前轮簇头选举概率。

本项目建议采用第一种方法，以保证每一轮仿真都能正常进行。

---

### 4.4 数据传输与聚合机制

LEACH 的通信过程主要分为两个阶段：

#### 4.4.1 普通节点向簇头发送数据

普通节点采集数据后，将数据发送给所属簇头。普通节点消耗发送能量，簇头消耗接收能量。

#### 4.4.2 簇头进行数据聚合并发送给基站

簇头收到簇内成员数据后，对数据进行聚合处理，然后将聚合后的数据发送给基站。簇头在此过程中需要消耗：

* 接收数据能量；
* 数据聚合能量；
* 向基站发送数据的能量。

由于簇头承担更多通信任务，因此簇头能量消耗通常大于普通节点。

---

## 五、能量模型设计

为了模拟节点能耗，本项目采用经典无线通信能量模型。

### 5.1 发送能量模型

节点发送 (k) bit 数据到距离为 (d) 的接收方时，发送能量为：

[
E_{TX}(k,d)=E_{elec} \times k + E_{amp} \times k \times d^2
]

其中：

* (E_{elec})：发送或接收电路能耗；
* (E_{amp})：功率放大器能耗；
* (k)：数据包大小；
* (d)：通信距离。

### 5.2 接收能量模型

节点接收 (k) bit 数据时，接收能量为：

[
E_{RX}(k)=E_{elec} \times k
]

### 5.3 数据聚合能量模型

簇头聚合 (k) bit 数据时，消耗能量为：

[
E_{DA}(k)=E_{DA} \times k
]

---

## 六、系统功能模块设计

本项目程序主要由以下模块组成。

### 6.1 节点类 Node

`Node` 类用于表示无线传感器网络中的单个节点。

主要属性包括：

```python
id          # 节点编号
x, y        # 节点坐标
energy      # 节点剩余能量
is_alive    # 节点是否存活
is_CH       # 节点是否为簇头
cluster_id  # 所属簇头编号
last_CH_round # 上一次成为簇头的轮次
```

---

### 6.2 网络初始化模块

该模块负责：

* 设置仿真区域大小；
* 设置节点数量；
* 随机生成节点位置；
* 设置基站位置；
* 初始化节点能量。

---

### 6.3 簇头选举模块

该模块负责：

* 根据当前轮数计算 LEACH 阈值；
* 判断节点是否满足成为簇头的条件；
* 选出当前轮簇头节点；
* 更新节点状态。

---

### 6.4 分簇模块

该模块负责：

* 遍历所有普通节点；
* 计算其到所有簇头的距离；
* 将其加入最近的簇头；
* 记录节点所属簇头编号。

---

### 6.5 通信与能耗更新模块

该模块负责：

* 普通节点向簇头发送数据；
* 簇头接收数据；
* 簇头进行数据聚合；
* 簇头向基站发送聚合数据；
* 更新所有节点的剩余能量；
* 判断节点是否死亡。

---

### 6.6 可视化模块

该模块负责绘制：

* 初始节点随机分布图；
* 每一轮的簇头和普通节点分布图；
* 分簇连线图；
* 每轮存活节点数量变化曲线；
* 网络总剩余能量变化曲线。

---

## 七、程序流程设计

整体程序流程如下：

```text
开始
 |
 |-- 初始化仿真参数
 |-- 随机生成传感器节点
 |-- 设置基站位置
 |
 |-- for 每一轮 round:
 |      |
 |      |-- 重置所有节点的簇头状态和所属簇
 |      |
 |      |-- 根据 LEACH 阈值选举簇头
 |      |
 |      |-- 如果没有簇头，则随机选择一个存活节点作为簇头
 |      |
 |      |-- 普通节点选择最近簇头并加入簇
 |      |
 |      |-- 普通节点向簇头发送数据
 |      |
 |      |-- 簇头接收并聚合数据
 |      |
 |      |-- 簇头向基站发送数据
 |      |
 |      |-- 更新节点能量和存活状态
 |      |
 |      |-- 记录当前轮存活节点数量和总能量
 |      |
 |      |-- 绘制当前轮分簇结果
 |
 |-- 绘制最终统计曲线
 |
结束
```

---

## 八、关键参数设置

| 参数名称           |      含义 |                           建议取值 |
| -------------- | ------: | -----------------------------: |
| `AREA_WIDTH`   |  仿真区域宽度 |                          100 m |
| `AREA_HEIGHT`  |  仿真区域高度 |                          100 m |
| `NUM_NODES`    | 传感器节点数量 |                            100 |
| `INIT_ENERGY`  |  节点初始能量 |                          0.5 J |
| `P`            |  期望簇头比例 |                           0.05 |
| `ROUNDS`       |    仿真轮数 |                            100 |
| `PACKET_SIZE`  |   数据包大小 |                       4000 bit |
| `E_ELEC`       |    电路能耗 |      (50 \times 10^{-9}) J/bit |
| `E_AMP`        |   放大器能耗 | (100 \times 10^{-12}) J/bit/m² |
| `E_DA`         |  数据聚合能耗 |       (5 \times 10^{-9}) J/bit |
| `BASE_STATION` |    基站坐标 |                    `(50, 150)` |

---

## 九、预期实验结果

本项目运行后，预期得到以下结果。

### 9.1 节点分布图

展示传感器节点在二维区域中的随机分布情况。

普通节点可以使用蓝色圆点表示，基站可以使用红色五角星表示。

---

### 9.2 LEACH 分簇结果图

展示某一轮中：

* 哪些节点被选为簇头；
* 普通节点加入了哪个簇；
* 普通节点与簇头之间的通信连接；
* 簇头与基站之间的通信连接。

建议图例设计如下：

| 图形元素  | 含义         |
| ----- | ---------- |
| 蓝色圆点  | 普通节点       |
| 红色三角形 | 簇头节点       |
| 黑色星号  | 基站         |
| 灰色虚线  | 普通节点到簇头的连接 |
| 红色实线  | 簇头到基站的连接   |

---

### 9.3 存活节点数量变化曲线

随着仿真轮数增加，节点能量逐渐消耗，存活节点数量会逐渐下降。

横轴为轮数，纵轴为存活节点数量。

---

### 9.4 网络总剩余能量变化曲线

随着通信过程不断进行，网络总剩余能量会逐渐下降。

横轴为轮数，纵轴为网络总剩余能量。

---

## 十、项目实现计划

| 阶段     | 主要任务                  | 预期成果                      |
| ------ | --------------------- | ------------------------- |
| 第 1 阶段 | 学习 WSN 和 LEACH 协议基本原理 | 理解分簇、簇头选举和数据聚合流程          |
| 第 2 阶段 | 搭建 Python 仿真环境        | 安装 `numpy` 和 `matplotlib` |
| 第 3 阶段 | 编写节点类和网络初始化代码         | 实现节点随机分布                  |
| 第 4 阶段 | 实现 LEACH 簇头选举算法       | 能够在每轮选出簇头节点               |
| 第 5 阶段 | 实现普通节点入簇算法            | 普通节点能够加入最近簇头              |
| 第 6 阶段 | 实现通信能耗模型              | 能够模拟节点能量变化                |
| 第 7 阶段 | 绘制分簇图和能量曲线            | 得到实验结果图                   |
| 第 8 阶段 | 整理代码和实验报告             | 完成最终作业提交材料                |

---

## 十一、项目创新点与扩展方向

本项目的基础目标是复现 LEACH 协议的核心流程。在完成基础功能后，可以进一步扩展以下内容：

1. 比较不同簇头概率 (p) 对网络生命周期的影响。

2. 比较基站位于区域中心和区域外部时的能耗差异。

3. 统计第一个节点死亡轮数、半数节点死亡轮数和全部节点死亡轮数。

4. 增加随机种子，使实验结果可复现。

5. 与普通直接传输协议进行对比，说明 LEACH 分簇聚合的节能效果。

6. 使用动态图展示每一轮簇结构变化。

---

## 十二、预期提交材料

本项目最终提交材料包括：

1. **Python 源代码文件**

   文件名建议：

   ```text
   leach_simulation.py
   ```

2. **运行结果截图**

   包括：

   * 节点初始分布图；
   * 某一轮 LEACH 分簇结果图；
   * 存活节点数量变化曲线；
   * 网络总剩余能量变化曲线。

3. **实验说明文档**

   内容包括：

   * 项目背景；
   * LEACH 原理；
   * 算法流程；
   * 参数设置；
   * 实验结果；
   * 总结分析。

4. **可选材料**

   * 动态分簇演示 GIF；
   * 多组参数对比结果；
   * 项目答辩 PPT。

---

## 十三、项目可行性分析

本项目难度适中，适合作为无线传感器网络或通信网络课程的课外作业。LEACH 协议结构清晰，主要涉及随机选举、距离计算、节点分组和能耗统计，使用 Python 可以较方便地实现。

所需第三方库较少，主要包括：

```bash
pip install numpy matplotlib
```

项目不依赖复杂的网络仿真平台，例如 NS-2、NS-3 或 OMNeT++，因此部署和调试成本较低。通过二维图像和曲线图展示结果，也便于观察算法运行过程和分析能耗变化趋势。

---

## 十四、可能存在的问题及解决方案

| 问题         | 原因                | 解决方案                           |
| ---------- | ----------------- | ------------------------------ |
| 某一轮没有选出簇头  | LEACH 选举具有随机性     | 随机选择一个存活节点作为簇头                 |
| 簇头能量消耗过快   | 簇头承担接收、聚合和远距离发送任务 | 每轮重新选举簇头，实现能量负载均衡              |
| 节点死亡后仍参与计算 | 没有及时更新节点状态        | 每轮开始前检查节点能量，死亡节点不参与选举和通信       |
| 图像显示混乱     | 节点数量多，连线复杂        | 只绘制部分轮次或降低连线透明度                |
| 实验结果每次不同   | 随机部署节点和随机选举簇头     | 设置随机种子，例如 `np.random.seed(42)` |

---

## 十五、总结

本项目基于 Python 实现无线传感器网络中的 LEACH 分簇聚合协议仿真。项目通过随机部署节点、周期性簇头选举、普通节点入簇、数据聚合和能量消耗建模，模拟 WSN 中分簇路由协议的基本工作过程。

通过本项目，可以理解 LEACH 协议如何利用簇头轮换机制降低通信能耗，并掌握无线传感器网络仿真中节点建模、距离计算、能量模型和结果可视化的基本方法。最终成果包括完整 Python 代码、节点分簇图、能量变化曲线和实验说明文档，能够满足课外作业的基本要求。

---

## 参考文献

[1] Heinzelman, W. R., Chandrakasan, A., & Balakrishnan, H. “Energy-Efficient Communication Protocol for Wireless Microsensor Networks.” *Proceedings of the 33rd Annual Hawaii International Conference on System Sciences*, 2000.

[2] Heinzelman, W. B., Chandrakasan, A. P., & Balakrishnan, H. “An Application-Specific Protocol Architecture for Wireless Microsensor Networks.” *IEEE Transactions on Wireless Communications*, 2002.

[3] Akkaya, K., & Younis, M. “A Survey on Routing Protocols for Wireless Sensor Networks.” *Ad Hoc Networks*, 2005.
